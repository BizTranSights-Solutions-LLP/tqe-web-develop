// import { Pipe, PipeTransform } from '@angular/core';
//
// @Pipe({
//     name: 'shown',
//     pure: false
// })
// export class ShownPipe implements PipeTransform {
//     transform(items: any[], filter: Object): any {
//         if (!items || items.length === 0) {
//             return items;
//         }
//
//         return items.filter(item => (item.shown === true && item.ignored == false));
//     }
// }
